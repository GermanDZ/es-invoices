"""URL routes for the certificate onboarding flow.

Stub bootstrapped by T-011 (Operation 1). The upload / replace / delete views
are wired here in Operation 5.
"""
from django.urls import path

app_name = "certificates"

urlpatterns = [
    # path("", views.upload, name="upload"),  # Operation 5
]
